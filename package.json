{
  "name": "@membrain/membrain",
  "version": "0.0.1",
  "description": "A JavaScript framework for syncronizing state across clients, servers, and databases",
  "main": "lib/index.js",
  "scripts": {
    "dev": "babel src -d lib --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d lib",
    "docs": "documentation build src/index.js -f html -o docs",
    "docs-w": "documentation serve --watch src/index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com//ben-turner/membrain.git"
  },
  "author": "Ben Turner",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.5.0",
    "@babel/core": "^7.5.0",
    "@babel/polyfill": "^7.4.4",
    "@babel/preset-env": "^7.5.0",
    "documentation": "^12.3.0"
  },
  "dependencies": {
    "core-js": "^3.1.4"
  }
}
